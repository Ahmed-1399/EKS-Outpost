apiVersion: v1
kind: Service
metadata:
  name: mongo-service
  namespace: abdelhamid-ns
spec:
  selector:
    app: mongo
  ports:
    - port: 27017
      targetPort: 27017
  clusterIP: None
---
apiVersion: apps/v1
# Statefulset
kind: StatefulSet
metadata:
  name: mongo
  namespace: abdelhamid-ns
spec:
  serviceName: "mongo-service"
  replicas: 1
  selector:
    matchLabels:
      app: mongo
  template:
    metadata:
      labels:
        app: mongo
    spec:
      containers:
        - name: mongo
          image: mongo:6.0
          ports:
            - containerPort: 27017
          volumeMounts:
            - name: mongo-storage
              mountPath: /data/db
  volumeClaimTemplates:
    - metadata:
        name: mongo-storage
      spec:
        accessModes: [ "ReadWriteOnce" ]
        storageClassName: "ebs-sc"
        resources:
          requests:
            storage: 5Gi

# aws eks create-addon --cluster-name EKS-on-Region --region us-west-2 --addon-name aws-ebs-csi-driver